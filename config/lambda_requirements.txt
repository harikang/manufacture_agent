# Lambda 공통 requirements

# AWS SDK
boto3==1.34.10

# Data processing
numpy==1.26.2

# SHAP for explainability
shap==0.44.0

# Visualization
matplotlib==3.8.2

# ML models (Lambda Layer로 배포 권장)
# torch==2.1.0  # CPU only version
# scikit-learn==1.3.2

# Note: PyTorch, scikit-learn, SHAP은 용량이 크므로 Lambda Layer로 별도 배포
# Lambda Layer 생성 방법:
# 1. pip install torch scikit-learn shap matplotlib -t python/ --platform manylinux2014_x86_64 --only-binary=:all:
# 2. zip -r layer.zip python/
# 3. AWS Lambda Layer로 업로드
