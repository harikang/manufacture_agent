FROM public.ecr.aws/lambda/python:3.11

# boto3 업데이트 (Bedrock Agent Runtime 지원)
RUN pip install --no-cache-dir --upgrade \
    boto3>=1.34.0 \
    botocore>=1.34.0

# Lambda 함수 코드 복사
COPY lambda_t3_rag.py ${LAMBDA_TASK_ROOT}/

# Handler 설정
CMD ["lambda_t3_rag.lambda_handler"]
