# ML 개발 및 배포 아키텍처

## 📋 전체 프로세스 개요

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        AI Engineer 로컬 개발 환경                              │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          모델 학습 및 검증                                      │
│  1. 데이터 분석 (EDA)                                                          │
│  2. Feature Engineering (AutoEncoder + Latent Features)                     │
│  3. 모델 학습 (Gradient Boosting, XGBoost, etc.)                             │
│  4. Ablation Study (최적 구성 탐색)                                            │
│  5. 성능 검증 (F1-Score, ROC-AUC, Accuracy)                                  │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          모델 Export                                          │
│  • autoencoder_latent12.pth (0.09 MB)                                       │
│  • gradient_boosting_model.pkl (1.27 MB)                                    │
│  • scaler.pkl (0.00 MB)                                                      │
│  Total: 1.37 MB                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                     Lambda 함수 패키징                                         │
│  • lambda_t1_predict.py (추론 코드)                                           │
│  • requirements.txt (의존성)                                                  │
│  • Dockerfile (컨테이너 이미지)                                                │
│  • models/ (학습된 모델 파일)                                                  │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                     AWS ECR (Container Registry)                             │
│  • Docker 이미지 Push                                                         │
│  • 버전 관리 (v1.0, v1.1, ...)                                                │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                     AWS Lambda 배포                                           │
│  • ECR 이미지 참조                                                             │
│  • 환경 변수 설정                                                              │
│  • IAM Role 설정                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                     프로덕션 서비스                                             │
│  Users → ALB → ECS (Streamlit) → API Gateway → Lambda T1 (Predict)         │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 🔬 Phase 1: AI Engineer 로컬 개발

### 1.1 데이터 분석 및 Feature Engineering

```
┌──────────────────────────────────────────────────────────────┐
│  AI Engineer 로컬 환경 (MacBook / Linux Workstation)          │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  📊 데이터 분석                                                │
│  ├─ DieCasting_Quality_Raw_Data.csv (7,535 samples)        │
│  ├─ 30 features (16 Process + 14 Sensor)                   │
│  ├─ 26 defect types                                         │
│  └─ 불량률: 22.42%                                           │
│                                                              │
│  🧪 Feature Engineering                                      │
│  ├─ AutoEncoder 설계                                         │
│  │   ├─ Architecture: 30 → 64 → 32 → 16 → 12 (latent)     │
│  │   ├─ SwiGLU Activation                                  │
│  │   ├─ 4-head Attention Module                            │
│  │   └─ Loss: MSE + Focal Loss                             │
│  │                                                          │
│  ├─ Latent Features 생성                                     │
│  │   ├─ 4D, 8D, 12D, 16D 차원 실험                          │
│  │   └─ 최적: 12D (F1: 0.7027)                              │
│  │                                                          │
│  └─ Combined Features                                       │
│      └─ 30D baseline + 12D latent = 42D total              │
│                                                              │
│  🤖 모델 학습                                                 │
│  ├─ Gradient Boosting (최종 선택)                            │
│  ├─ XGBoost                                                 │
│  ├─ LightGBM                                                │
│  ├─ CatBoost                                                │
│  ├─ Random Forest                                           │
│  └─ FT-Transformer / TabTransformer (실험)                  │
│                                                              │
│  📈 Ablation Study                                           │
│  ├─ Latent Dimension: 4D, 8D, 12D, 16D                     │
│  ├─ ML Models: 6가지 비교                                    │
│  ├─ Transformer Models: 2가지 비교                           │
│  └─ Feature Selection: 15D, 20D, 25D, 30D                  │
│                                                              │
│  ✅ 최종 선정                                                 │
│  ├─ Model: Gradient Boosting                               │
│  ├─ Features: 30D + 12D = 42D                              │
│  ├─ F1-Score: 0.7027                                       │
│  ├─ ROC-AUC: 0.9175                                        │
│  └─ Accuracy: 0.8832                                       │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

### 1.2 사용된 도구 및 라이브러리

```python
# 데이터 분석
pandas==2.1.3
numpy==1.24.3
matplotlib==3.8.2
seaborn==0.13.0

# 딥러닝 (AutoEncoder)
torch==2.1.0
torchvision==0.16.0

# 머신러닝
scikit-learn==1.3.2
xgboost==2.0.2
lightgbm==4.1.0
catboost==1.2.2

# Feature Importance
shap==0.43.0

# 실험 관리
jupyter==1.0.0
```

---

## 🚀 Phase 2: 모델 Export 및 패키징

### 2.1 모델 파일 Export

```
┌──────────────────────────────────────────────────────────────┐
│  export_models_for_deployment.py                             │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  # AutoEncoder Export                                        │
│  torch.save(autoencoder.state_dict(),                       │
│             'autoencoder_latent12.pth')                      │
│                                                              │
│  # Gradient Boosting Export                                 │
│  with open('gradient_boosting_model.pkl', 'wb') as f:       │
│      pickle.dump(gb_model, f)                               │
│                                                              │
│  # Scaler Export                                            │
│  with open('scaler.pkl', 'wb') as f:                        │
│      pickle.dump(scaler, f)                                 │
│                                                              │
└──────────────────────────────────────────────────────────────┘
                        │
                        ▼
┌──────────────────────────────────────────────────────────────┐
│  deployment_models/                                          │
│  ├─ autoencoder_latent12.pth      0.09 MB                   │
│  ├─ gradient_boosting_model.pkl   1.27 MB                   │
│  └─ scaler.pkl                     0.00 MB                   │
│  ─────────────────────────────────────────                   │
│  Total                             1.37 MB                   │
└──────────────────────────────────────────────────────────────┘
```

### 2.2 Lambda 함수 구현

```
┌──────────────────────────────────────────────────────────────┐
│  lambda_t1_predict.py                                        │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  class AutoEncoder(nn.Module):                              │
│      """30D → 12D latent features"""                        │
│      def __init__(self, input_dim=30, latent_dim=12):       │
│          # Encoder: 30 → 64 → 32 → 16 → 12                 │
│          # Attention: 4-head                                │
│          # Decoder: 12 → 16 → 32 → 64 → 30                 │
│                                                              │
│  def load_models():                                         │
│      """Cold Start 시 모델 로딩"""                            │
│      autoencoder = AutoEncoder()                            │
│      autoencoder.load_state_dict(torch.load(...))           │
│      gb_model = pickle.load(...)                            │
│      scaler = pickle.load(...)                              │
│                                                              │
│  def predict(X):                                            │
│      """추론 파이프라인"""                                     │
│      # 1. Scaling (30D)                                     │
│      X_scaled = scaler.transform(X)                         │
│                                                              │
│      # 2. Latent features 추출 (12D)                         │
│      latent = autoencoder.encode(X_scaled)                  │
│                                                              │
│      # 3. Combined features (42D)                           │
│      X_combined = np.hstack([X_scaled, latent])             │
│                                                              │
│      # 4. Gradient Boosting 예측                            │
│      proba = gb_model.predict_proba(X_combined)             │
│                                                              │
│      return prediction                                      │
│                                                              │
│  def lambda_handler(event, context):                        │
│      """Lambda 엔트리포인트"""                                 │
│      load_models()  # Cold Start                            │
│      result = predict(event['features'])                    │
│      return result                                          │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

## 🐳 Phase 3: Docker 컨테이너화

### 3.1 Dockerfile

```dockerfile
┌──────────────────────────────────────────────────────────────┐
│  Dockerfile                                                  │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  FROM public.ecr.aws/lambda/python:3.11                     │
│                                                              │
│  # 모델 파일 복사                                             │
│  COPY models/autoencoder_latent12.pth /opt/ml/models/       │
│  COPY models/gradient_boosting_model.pkl /opt/ml/models/    │
│  COPY models/scaler.pkl /opt/ml/models/                     │
│                                                              │
│  # 의존성 설치                                                │
│  COPY requirements.txt .                                     │
│  RUN pip install --no-cache-dir -r requirements.txt         │
│                                                              │
│  # Lambda 함수 복사                                           │
│  COPY lambda_t1_predict.py ${LAMBDA_TASK_ROOT}/             │
│                                                              │
│  CMD ["lambda_t1_predict.lambda_handler"]                   │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

### 3.2 Docker 빌드 및 Push

```bash
# 1. Docker 이미지 빌드
docker build -t diecasting-predict-t1:v1.0 .

# 2. ECR 로그인
aws ecr get-login-password --region ap-northeast-2 | \
  docker login --username AWS --password-stdin \
  <account-id>.dkr.ecr.ap-northeast-2.amazonaws.com

# 3. 이미지 태깅
docker tag diecasting-predict-t1:v1.0 \
  <account-id>.dkr.ecr.ap-northeast-2.amazonaws.com/diecasting-predict-t1:v1.0

# 4. ECR에 Push
docker push \
  <account-id>.dkr.ecr.ap-northeast-2.amazonaws.com/diecasting-predict-t1:v1.0
```

---

## ☁️ Phase 4: AWS Lambda 배포

### 4.1 Lambda 함수 생성

```bash
# Lambda 함수 생성 (ECR 이미지 사용)
aws lambda create-function \
  --function-name diecasting-predict-t1 \
  --package-type Image \
  --code ImageUri=<account-id>.dkr.ecr.ap-northeast-2.amazonaws.com/diecasting-predict-t1:v1.0 \
  --role arn:aws:iam::<account-id>:role/lambda-execution-role \
  --memory-size 2048 \
  --timeout 30 \
  --environment Variables={MODEL_VERSION=v1.0}
```

### 4.2 Lambda 구성

```
┌──────────────────────────────────────────────────────────────┐
│  Lambda T1 Configuration                                     │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  Function Name: diecasting-predict-t1                       │
│  Runtime: Python 3.11 (Container)                           │
│  Memory: 2048 MB                                            │
│  Timeout: 30 seconds                                        │
│  Architecture: x86_64                                       │
│                                                              │
│  Environment Variables:                                     │
│  ├─ MODEL_VERSION=v1.0                                      │
│  ├─ MODEL_PATH=/opt/ml/models                               │
│  └─ LOG_LEVEL=INFO                                          │
│                                                              │
│  IAM Role:                                                  │
│  ├─ AWSLambdaBasicExecutionRole                             │
│  ├─ AWSLambdaVPCAccessExecutionRole                         │
│  └─ CloudWatch Logs 권한                                     │
│                                                              │
│  VPC Configuration:                                         │
│  ├─ VPC: vpc-xxx                                            │
│  ├─ Subnets: subnet-xxx (Private)                           │
│  └─ Security Groups: sg-xxx                                 │
│                                                              │
│  Performance:                                               │
│  ├─ Cold Start: ~2s (모델 로딩)                              │
│  ├─ Warm Execution: ~15ms                                   │
│  └─ Concurrent Executions: 100                              │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

## 🔄 Phase 5: 프로덕션 서비스 통합

### 5.1 전체 아키텍처

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              사내 사용자                                        │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ HTTPS (사내 IP Allowlist)
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                     Application Load Balancer (ALB)                          │
│  • IP Allowlist: 사내 IP만 허용                                                │
│  • SSL/TLS Termination                                                       │
│  • Health Check                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                     ECS Fargate (Private Subnet)                             │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  Streamlit UI + Agent (오케스트레이터)                                    │  │
│  │  • 사용자 인터페이스                                                       │  │
│  │  • 입력 데이터 수집 (30 features)                                         │  │
│  │  • API 호출 관리                                                          │  │
│  │  • 결과 시각화                                                            │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ HTTPS (IAM Auth)
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          API Gateway (REST API)                              │
│  • IAM Authentication                                                        │
│  • Rate Limiting                                                             │
│  • Request/Response Transformation                                           │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                    ┌───────────────┼───────────────┐
                    │               │               │
                    ▼               ▼               ▼
        ┌───────────────┐ ┌───────────────┐ ┌───────────────┐
        │  Lambda T1    │ │  Lambda T2    │ │  Lambda T3    │
        │  Predict      │ │  Importance   │ │  RAG Query    │
        └───────────────┘ └───────────────┘ └───────────────┘
                │               │               │
                │               ▼               ▼
                │         ┌─────────┐     ┌──────────┐
                │         │   S3    │     │ Bedrock  │
                │         │ Bucket  │     │    KB    │
                │         └─────────┘     └──────────┘
                │
                ▼
        ┌───────────────────────────────────────────────────┐
        │  Lambda T1 내부 처리 (AI Engineer가 개발한 모델)     │
        ├───────────────────────────────────────────────────┤
        │                                                   │
        │  1. 입력 데이터 수신 (30 features)                  │
        │     ├─ Process features (16개)                    │
        │     └─ Sensor features (14개)                     │
        │                                                   │
        │  2. Feature Scaling                               │
        │     └─ StandardScaler (학습 시 fit된 scaler)       │
        │                                                   │
        │  3. Latent Features 추출 (AutoEncoder)             │
        │     ├─ Input: 30D                                 │
        │     ├─ Encoder: 30 → 64 → 32 → 16 → 12           │
        │     ├─ Attention: 4-head self-attention           │
        │     └─ Output: 12D latent features                │
        │                                                   │
        │  4. Feature Combination                           │
        │     └─ 30D baseline + 12D latent = 42D total      │
        │                                                   │
        │  5. Gradient Boosting 예측                         │
        │     ├─ 200 estimators                             │
        │     ├─ max_depth=6                                │
        │     └─ Output: 불량 확률 (0~1)                     │
        │                                                   │
        │  6. 결과 반환                                       │
        │     ├─ prediction: defect / normal                │
        │     ├─ probability: 0.78                          │
        │     ├─ confidence: high / medium / low            │
        │     ├─ latent_features: [12D vector]              │
        │     ├─ attention_weights: [attention scores]      │
        │     └─ processing_time_ms: 15ms                   │
        │                                                   │
        └───────────────────────────────────────────────────┘
```

### 5.2 Lambda T1 상세 처리 흐름

```
┌─────────────────────────────────────────────────────────────┐
│  Lambda T1: Predict Function                                │
│  (AI Engineer가 로컬에서 학습한 모델 실행)                      │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Cold Start (최초 1회)                                │   │
│  │  ├─ AutoEncoder 로딩 (0.09 MB)                       │   │
│  │  ├─ Gradient Boosting 로딩 (1.27 MB)                 │   │
│  │  ├─ Scaler 로딩 (0.00 MB)                            │   │
│  │  └─ 소요 시간: ~2s                                    │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Warm Execution (이후 요청)                           │   │
│  │                                                       │   │
│  │  1. 입력 데이터 파싱                                   │   │
│  │     {                                                 │   │
│  │       "features": {                                   │   │
│  │         "Process_Temperature": 650.5,                 │   │
│  │         "Process_Pressure": 120.3,                    │   │
│  │         ...                                           │   │
│  │       }                                               │   │
│  │     }                                                 │   │
│  │                                                       │   │
│  │  2. Feature Extraction (30D)                          │   │
│  │     X = [650.5, 120.3, 2.5, ...]  # 30 features      │   │
│  │                                                       │   │
│  │  3. Scaling                                           │   │
│  │     X_scaled = scaler.transform(X)                    │   │
│  │                                                       │   │
│  │  4. AutoEncoder Inference                             │   │
│  │     X_tensor = torch.FloatTensor(X_scaled)            │   │
│  │     latent, attention = autoencoder.encode(X_tensor)  │   │
│  │     # latent: [12D vector]                            │   │
│  │                                                       │   │
│  │  5. Feature Combination                               │   │
│  │     X_combined = np.hstack([X_scaled, latent])        │   │
│  │     # X_combined: [42D vector]                        │   │
│  │                                                       │   │
│  │  6. Gradient Boosting Prediction                      │   │
│  │     proba = gb_model.predict_proba(X_combined)        │   │
│  │     pred_class = gb_model.predict(X_combined)         │   │
│  │                                                       │   │
│  │  7. 결과 생성                                          │   │
│  │     {                                                 │   │
│  │       "prediction": {                                 │   │
│  │         "class": "defect",                            │   │
│  │         "probability": 0.78,                          │   │
│  │         "confidence": "high"                          │   │
│  │       },                                              │   │
│  │       "latent_features": [0.12, -0.45, ...],          │   │
│  │       "attention_weights": [...],                     │   │
│  │       "processing_time_ms": 15,                       │   │
│  │       "model_version": "v1.0_12D_GB",                 │   │
│  │       "model_performance": {                          │   │
│  │         "f1_score": 0.7027,                           │   │
│  │         "roc_auc": 0.9175,                            │   │
│  │         "accuracy": 0.8832                            │   │
│  │       }                                               │   │
│  │     }                                                 │   │
│  │                                                       │   │
│  │  소요 시간: ~15ms                                      │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 Phase 6: 모델 업데이트 프로세스

### 6.1 모델 재학습 및 배포 워크플로우

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  Step 1: AI Engineer 로컬에서 모델 재학습                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  1. 새로운 데이터 수집                                                          │
│     └─ 프로덕션 환경에서 수집된 실제 데이터                                        │
│                                                                             │
│  2. 데이터 전처리 및 Feature Engineering                                       │
│     ├─ 기존 AutoEncoder 재사용 또는 재학습                                      │
│     └─ Latent features 생성                                                 │
│                                                                             │
│  3. 모델 재학습                                                               │
│     ├─ Hyperparameter Tuning                                               │
│     ├─ Cross-validation                                                    │
│     └─ 성능 검증                                                             │
│                                                                             │
│  4. A/B 테스트 준비                                                           │
│     ├─ 기존 모델 (v1.0): F1=0.7027                                           │
│     └─ 새 모델 (v1.1): F1=0.7150 (예시)                                      │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  Step 2: 모델 Export 및 버전 관리                                              │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  deployment_models/                                                         │
│  ├─ v1.0/                                                                   │
│  │   ├─ autoencoder_latent12.pth                                            │
│  │   ├─ gradient_boosting_model.pkl                                         │
│  │   └─ scaler.pkl                                                          │
│  │                                                                           │
│  └─ v1.1/  (새 버전)                                                         │
│      ├─ autoencoder_latent12.pth                                            │
│      ├─ gradient_boosting_model.pkl                                         │
│      └─ scaler.pkl                                                          │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  Step 3: Docker 이미지 빌드 및 Push                                            │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  # v1.1 이미지 빌드                                                           │
│  docker build -t diecasting-predict-t1:v1.1 .                               │
│                                                                             │
│  # ECR에 Push                                                               │
│  docker push <account-id>.dkr.ecr.ap-northeast-2.amazonaws.com/\           │
│    diecasting-predict-t1:v1.1                                               │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  Step 4: Lambda 함수 업데이트                                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  Option A: Blue/Green Deployment                                           │
│  ├─ Lambda T1 v1.0 (기존)                                                   │
│  ├─ Lambda T1 v1.1 (새 버전)                                                │
│  └─ API Gateway에서 트래픽 분산 (90% v1.0, 10% v1.1)                          │
│                                                                             │
│  Option B: Canary Deployment                                               │
│  ├─ Lambda Alias 사용                                                       │
│  ├─ 점진적 트래픽 전환 (0% → 10% → 50% → 100%)                               │
│  └─ CloudWatch 메트릭 모니터링                                                │
│                                                                             │
│  Option C: 직접 업데이트                                                      │
│  └─ aws lambda update-function-code \                                      │
│      --function-name diecasting-predict-t1 \                                │
│      --image-uri <ECR-URI>:v1.1                                             │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  Step 5: 모니터링 및 검증                                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  CloudWatch Metrics:                                                        │
│  ├─ Invocation Count                                                        │
│  ├─ Error Rate                                                              │
│  ├─ Duration (P50, P95, P99)                                                │
│  └─ Concurrent Executions                                                   │
│                                                                             │
│  Custom Metrics:                                                            │
│  ├─ Prediction Accuracy (실제 vs 예측)                                        │
│  ├─ F1-Score (실시간 계산)                                                    │
│  ├─ False Positive Rate                                                     │
│  └─ False Negative Rate                                                     │
│                                                                             │
│  Rollback 조건:                                                              │
│  ├─ Error Rate > 5%                                                         │
│  ├─ Duration P95 > 100ms                                                    │
│  └─ F1-Score < 0.65                                                         │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 📊 Phase 7: 모니터링 및 피드백 루프

### 7.1 프로덕션 모니터링

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  CloudWatch Dashboard                                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  Lambda T1 Metrics:                                                         │
│  ├─ Invocations: 10,000 / day                                               │
│  ├─ Error Rate: 0.5%                                                        │
│  ├─ Duration P50: 15ms                                                      │
│  ├─ Duration P95: 45ms                                                      │
│  ├─ Duration P99: 80ms                                                      │
│  ├─ Cold Start Rate: 2%                                                     │
│  └─ Concurrent Executions: 50                                               │
│                                                                             │
│  Model Performance:                                                         │
│  ├─ Prediction Accuracy: 88.5%                                              │
│  ├─ F1-Score: 0.7027                                                        │
│  ├─ Precision: 0.82                                                         │
│  ├─ Recall: 0.58                                                            │
│  ├─ False Positive Rate: 5.2%                                               │
│  └─ False Negative Rate: 6.3%                                               │
│                                                                             │
│  Cost Metrics:                                                              │
│  ├─ Lambda Cost: $10.42 / month                                             │
│  ├─ API Gateway Cost: $3.50 / month                                         │
│  ├─ ECS Cost: $50.00 / month                                                │
│  └─ Total: $63.92 / month                                                   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 7.2 데이터 수집 및 피드백

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  Feedback Loop                                                               │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  1. 예측 결과 저장 (S3)                                                        │
│     ├─ 입력 features (30D)                                                   │
│     ├─ 예측 결과 (defect / normal)                                           │
│     ├─ 예측 확률                                                             │
│     └─ 타임스탬프                                                             │
│                                                                             │
│  2. 실제 결과 수집 (Ground Truth)                                             │
│     ├─ 품질 검사 결과                                                         │
│     ├─ 불량 유형                                                             │
│     └─ 검사 시간                                                             │
│                                                                             │
│  3. 성능 분석                                                                 │
│     ├─ Confusion Matrix                                                     │
│     ├─ Feature Drift 감지                                                    │
│     ├─ Concept Drift 감지                                                    │
│     └─ 성능 저하 알림                                                         │
│                                                                             │
│  4. 재학습 트리거                                                             │
│     ├─ 성능 저하 > 5%                                                        │
│     ├─ 데이터 Drift 감지                                                     │
│     ├─ 새로운 데이터 1,000건 이상                                             │
│     └─ 월 1회 정기 재학습                                                     │
│                                                                             │
│  5. AI Engineer에게 알림                                                      │
│     └─ 재학습 필요 시 이메일/Slack 알림                                         │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 🎯 핵심 포인트 요약

### AI Engineer의 역할

1. **로컬 개발 환경에서 모델 개발**
   - 데이터 분석 및 Feature Engineering
   - AutoEncoder 설계 및 학습 (30D → 12D)
   - Gradient Boosting 모델 학습
   - Ablation Study를 통한 최적 구성 탐색

2. **모델 Export 및 패키징**
   - PyTorch 모델 (.pth) 및 scikit-learn 모델 (.pkl) Export
   - Lambda 함수 코드 작성 (추론 파이프라인)
   - Docker 컨테이너화

3. **AWS 배포**
   - ECR에 Docker 이미지 Push
   - Lambda 함수 생성 및 구성
   - 성능 모니터링 및 최적화

4. **지속적 개선**
   - 프로덕션 데이터 수집 및 분석
   - 모델 재학습 및 업데이트
   - A/B 테스트 및 성능 비교

### Lambda T1의 역할

1. **추론 서비스 제공**
   - 30D features 입력 받기
   - AutoEncoder로 12D latent features 추출
   - Gradient Boosting으로 불량 예측
   - 결과 반환 (예측, 확률, 신뢰도)

2. **성능 최적화**
   - Cold Start 최소화 (모델 캐싱)
   - Warm Execution 최적화 (~15ms)
   - 동시 실행 관리

3. **모니터링 및 로깅**
   - CloudWatch Logs
   - Custom Metrics
   - Error Tracking

---

## 📚 참고 자료

### 생성된 파일

- `autoencoder_model.py` - AutoEncoder 구현
- `ml_classification.py` - ML 모델 학습 및 평가
- `export_models_for_deployment.py` - 모델 Export 스크립트
- `lambda_t1_predict.py` - Lambda 함수 코드
- `deployment_models/` - 배포용 모델 파일
- `FINAL_ABLATION_REPORT.md` - Ablation Study 결과
- `POC_SUMMARY.md` - PoC 전체 요약

### 관련 문서

- AWS Lambda 문서: https://docs.aws.amazon.com/lambda/
- PyTorch 문서: https://pytorch.org/docs/
- scikit-learn 문서: https://scikit-learn.org/

---

**작성일**: 2026-01-16  
**버전**: v1.0  
**작성자**: AI Assistant
