# ML 개발 및 배포 플로우 (간단 버전)

## 🔄 전체 프로세스

```
┌──────────────────────────────────────────────────────────────────┐
│  Phase 1: AI Engineer 로컬 개발                                    │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  📊 데이터 분석                                                    │
│  └─ DieCasting_Quality_Raw_Data.csv (7,535 samples, 30 features)│
│                                                                  │
│  🧪 Feature Engineering                                          │
│  ├─ AutoEncoder: 30D → 12D latent                               │
│  └─ Combined: 30D + 12D = 42D                                   │
│                                                                  │
│  🤖 모델 학습                                                      │
│  ├─ Gradient Boosting (최종 선택)                                 │
│  ├─ XGBoost, LightGBM, CatBoost                                 │
│  └─ Ablation Study                                              │
│                                                                  │
│  ✅ 최종 모델                                                      │
│  └─ F1: 0.7027, ROC-AUC: 0.9175, Acc: 0.8832                   │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
                        │
                        ▼
┌──────────────────────────────────────────────────────────────────┐
│  Phase 2: 모델 Export                                             │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  deployment_models/                                              │
│  ├─ autoencoder_latent12.pth      (0.09 MB)                     │
│  ├─ gradient_boosting_model.pkl   (1.27 MB)                     │
│  └─ scaler.pkl                     (0.00 MB)                     │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
                        │
                        ▼
┌──────────────────────────────────────────────────────────────────┐
│  Phase 3: Lambda 함수 구현                                         │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  lambda_t1_predict.py                                            │
│  ├─ load_models() - 모델 로딩                                     │
│  ├─ extract_features() - 30D features 추출                       │
│  ├─ predict() - 추론 파이프라인                                    │
│  └─ lambda_handler() - Lambda 엔트리포인트                        │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
                        │
                        ▼
┌──────────────────────────────────────────────────────────────────┐
│  Phase 4: Docker 컨테이너화                                        │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Dockerfile                                                      │
│  ├─ Base: public.ecr.aws/lambda/python:3.11                     │
│  ├─ 모델 파일 복사                                                 │
│  ├─ 의존성 설치 (torch, scikit-learn)                             │
│  └─ Lambda 함수 복사                                              │
│                                                                  │
│  docker build -t diecasting-predict-t1:v1.0 .                    │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
                        │
                        ▼
┌──────────────────────────────────────────────────────────────────┐
│  Phase 5: AWS ECR Push                                           │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  docker push <account-id>.dkr.ecr.ap-northeast-2.amazonaws.com/ │
│    diecasting-predict-t1:v1.0                                    │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
                        │
                        ▼
┌──────────────────────────────────────────────────────────────────┐
│  Phase 6: Lambda 배포                                             │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  aws lambda create-function \                                    │
│    --function-name diecasting-predict-t1 \                       │
│    --package-type Image \                                        │
│    --code ImageUri=<ECR-URI>:v1.0 \                              │
│    --memory-size 2048 \                                          │
│    --timeout 30                                                  │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
                        │
                        ▼
┌──────────────────────────────────────────────────────────────────┐
│  Phase 7: 프로덕션 서비스                                          │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Users → ALB → ECS (Streamlit) → API Gateway → Lambda T1        │
│                                                                  │
│  Lambda T1 내부:                                                  │
│  1. 30D features 입력                                             │
│  2. Scaling                                                      │
│  3. AutoEncoder → 12D latent                                     │
│  4. Combine → 42D                                                │
│  5. Gradient Boosting → 예측                                      │
│  6. 결과 반환                                                      │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

---

## 🎯 핵심 요약

### AI Engineer가 하는 일
1. 로컬에서 데이터 분석 및 모델 학습
2. 최적 모델 선정 (Ablation Study)
3. 모델 파일 Export
4. Lambda 함수 코드 작성
5. Docker 이미지 빌드 및 배포

### Lambda T1이 하는 일
1. 모델 로딩 (Cold Start)
2. 입력 데이터 처리 (30D features)
3. AutoEncoder로 latent features 추출 (12D)
4. Gradient Boosting으로 예측
5. 결과 반환 (예측, 확률, 신뢰도)

---

**상세 내용은 `ML_DEVELOPMENT_ARCHITECTURE.md` 참고**
